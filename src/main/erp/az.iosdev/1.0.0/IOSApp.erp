entity IOSApp {
  field title: String
  field code: Int
  //system field reviewRetriever: ReviewRetriever inline by app
}

object IOSService extends SystemService {
  func title = "iOS Service"
  func roles = [Admin]
}

extend entity Ticket extends NewEngine