entity Company {
  field code: Int
  field name: String
  field description: String
  
  view form = column {
    label("Main fields")
    grid {
      code, name
    }
    label("Some description about form")
  }
}