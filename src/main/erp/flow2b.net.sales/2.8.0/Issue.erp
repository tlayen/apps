extend trait Issue {
  const field quotes: [DevQuoteItem]? by issues
}

extend entity Bug {
  extend view form = quotes
}
extend entity UserStory {
  extend view form = quotes
}
extend entity TechTask {
  extend view form = quotes
}
extend entity Incident {
  extend view form = quotes
}
extend entity DevTask {
  extend view form = quotes
}