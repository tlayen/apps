class IR10Y14Income {
  field f2: Money[NZD]? {label = "2: Gross Income from Gross income"}
  back func calc(ir: IncomeTaxReturn): Future[Void] = {
    let le = ir.legalEntity
    let p = ir.period
    
    //let m <- IncomeStatement.subAccountTypes
    //let mapping: [LedgerAccountSubType]
  }
}
