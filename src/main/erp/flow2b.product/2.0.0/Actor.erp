extend trait Actor {
  field pricing: ProductPricing? inline by vendor  
}

extend entity Company {
  extend view form = pricing
}

extend entity Person {
  extend view form = pricing
}

extend entity CompanyGroup {
  extend view form = pricing
}