enum MetalType(currency: Currency?){
  case Gold(currency = XAU) {
    background = "#fe8"
  }
  case Silver(currency = XAG) {
    background = "#bbb"
  }
  case Platinum(currency = XPT) {
    background = "#eee"
  }
  case Palladium(currency = null) {
    background = "#fae"
  }
}